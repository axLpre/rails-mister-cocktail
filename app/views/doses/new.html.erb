<div class="container">
  <h3>New Dose for <%= @cocktail.name %></h3>

  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.input :description %>
    <%= f.association :ingredient , as: :radio_buttons %>
    <%= f.submit %>
  <% end %>
</div>
