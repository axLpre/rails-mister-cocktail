<%= simple_form_for cocktail do |f| %>

  <%= f.input :name %>

  <%= f.input :ingredients, as: :select, collection: ["lemon", "ice", "mint leaves"] %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit %>
<% end %>